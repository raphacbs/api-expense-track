apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: expensetrackapi-ingress
spec:
  rules:
    - host: localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: expensetrackapi
                port:
                  number: 8082
